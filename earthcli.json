{
  "ignore": ["README.md", ".gitignore"],
  "commands": {
    "Compile": "earthcli compile",
    "Migrate": "earthcli migrate",
    "Test contracts": "earthcli test"
  },
  "hooks": {
    "post-unpack": "bash post-unpack.sh && rm post-unpack.sh && npm install"
  }
}
